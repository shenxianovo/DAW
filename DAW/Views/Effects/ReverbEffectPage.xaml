<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DAW.Views.Effects.ReverbEffectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DAW.Views.Effects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="15, 0, 15, 0">
        <TextBlock Text="Reverb" Margin="0, 5"/>

        <StackPanel>
            <TextBlock Text="湿信号比例 (WetMix)" Margin="0,10,0,4"/>
            <Slider Minimum="0" Maximum="1" StepFrequency="0.01"
                    Value="{x:Bind ViewModel.WetMix, Mode=TwoWay}"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="衰减时间 (Decay)" Margin="0,10,0,4"/>
            <Slider Minimum="0" Maximum="0.95" StepFrequency="0.01"
                    Value="{x:Bind ViewModel.Decay, Mode=TwoWay}"/>
        </StackPanel>
    </StackPanel>
</Page>
